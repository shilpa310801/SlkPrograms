class Student{ //memebers can be accessed from diff classes within one package
}

public class Student{ //members outside the package can also access the variables
}

. -> member access operator

Scanner sc= new Scanner();
e1.id= sc.nextInt();
